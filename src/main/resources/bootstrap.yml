spring:
  cloud:
    kubernetes:
      config:
        namespace: param-env
        sources:
          - name: service-name
      reload:
        enabled: true
        period: 10000
        mode: polling